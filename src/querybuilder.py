from dataclasses import dataclass

@dataclass
class QueryBuilder:
    # Used to build queries to submit to run_query(). Class used to construct fieldpaths params for querying    
    # What query fields to query?
    # What order?
    pass